version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - yum install epel-release
      - yum install python-pip
      - pip install -r requirements.txt
  build:
    commands:
      - python calc.py
artifacts:
  files: 
    - "**/*"
  base-directory: ../sources